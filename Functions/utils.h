#pragma once

void hello();
int square(int);
float decimal(int numerator, int denominator);

void printPair(int, int);

int sumThree(int, int, int);

int min(int, int);
int max(int, int);

int clamp(int lower, int upper, int);

float distance(float x1, float y1, float x2, float y2);

int estTimeToArrival(int x1, int y1, int x2, int y2, int speed);